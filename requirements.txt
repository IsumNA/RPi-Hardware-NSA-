# NSA Optimization Stack - prototype demo dependencies
# Tested on Python 3.13 (CPU only)
#
# Install with:   pip install -r requirements.txt
# Linux GUI also needs Tk (not a pip package):   sudo apt install python3-tk

numpy>=2.0
opencv-python>=4.8
matplotlib>=3.8
Pillow>=10.0
PyYAML>=6.0
rich>=13.0
torch>=2.2
onnx>=1.15

# Optional: running Hugging Face ONNX models directly.
# onnxruntime>=1.17
#
# Optional: NAFNet models from deepghs/image_restoration (recommended Hub source).
# dghs-imgutils>=0.19

# Optional: enables loading real .dng captures (e.g. denoise-hw PI_RAW datasets).
# .npy / .png / .tif inputs work without it.
# rawpy>=0.19

# Optional: enables the Optuna TPE search in search.py (--optuna N).
# Without it the sweep falls back to an exhaustive grid search.
# optuna>=3.6

# Optional: enables downloading a pinned snapshot when freezing a Hugging Face
# model (hf_search.py --download / the GUI FREEZE button). Searching, license
# filtering and freezing the commit hash work without it (stdlib HTTP only).
# huggingface_hub>=0.23

# Raspberry Pi CSI camera (live testing) — usually NO sudo needed
#
# On Raspberry Pi OS, python3-picamera2 is typically PRE-INSTALLED. You do not
# need sudo apt if you let your venv see system packages:
#
#     python3 -m venv --system-site-packages .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
#
# Or edit .venv/pyvenv.cfg →  include-system-site-packages = true
#
# Diagnose what works on your Pi (no sudo):
#     python pi_camera_check.py
#
# live.py also tries rpicam-vid / libcamera-vid (preinstalled on Pi OS) when
# picamera2 is not importable. Pip-only fallback (no apt):
#     pip install -r requirements-pi.txt
