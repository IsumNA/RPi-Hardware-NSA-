# NSA Optimization Stack - prototype demo dependencies
# Tested on Python 3.13 (CPU only)
#
# Install with:   pip install -r requirements.txt
# Linux GUI also needs Tk (not a pip package):   sudo apt install python3-tk

numpy>=2.0
opencv-python>=4.8
matplotlib>=3.8
Pillow>=10.0
PyYAML>=6.0
rich>=13.0
torch>=2.2
onnx>=1.15

# Optional: running Hugging Face ONNX models directly.
# onnxruntime>=1.17
#
# Optional: NAFNet models from deepghs/image_restoration (recommended Hub source).
# dghs-imgutils>=0.19

# Optional: enables loading real .dng captures (e.g. denoise-hw PI_RAW datasets).
# .npy / .png / .tif inputs work without it.
# rawpy>=0.19

# Optional: enables the Optuna TPE search in search.py (--optuna N).
# Without it the sweep falls back to an exhaustive grid search.
# optuna>=3.6

# Downloading a pinned snapshot when freezing a Hugging Face model
# (hf_search.py --download / the GUI DOWNLOAD & USE button). Searching, license
# filtering and freezing the commit hash work without it (stdlib HTTP only), but
# actually pulling the weights needs this package.
huggingface_hub>=0.23

# Raspberry Pi CSI camera (Python stack)
#
# picamera2 + libcamera SHIP WITH Pi OS — do NOT pip-install them (the wheels
# often don't exist and pip errors with "could not find a version..."). Instead
# let your Pi venv see the preinstalled system packages:
#     python3 -m venv --system-site-packages .venv
#
# The Pi's live-testing pip extras (OpenCV) live in requirements-pi.txt:
#     pip install -r requirements-pi.txt
# Check:  python pi_camera_check.py
