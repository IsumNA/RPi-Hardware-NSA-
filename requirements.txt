# NSA Optimization Stack - prototype demo dependencies
# Tested on Python 3.13 (CPU only)
#
# Install with:   pip install -r requirements.txt
# Linux GUI also needs Tk (not a pip package):   sudo apt install python3-tk

numpy>=2.0
opencv-python>=4.8
matplotlib>=3.8
Pillow>=10.0
PyYAML>=6.0
rich>=13.0
torch>=2.2
onnx>=1.15

# Optional: enables running inference directly from the exported .onnx graph.
# The demo falls back to the live torch runtime if this is not installed.
# onnxruntime>=1.17

# Optional: enables loading real .dng captures (e.g. denoise-hw PI_RAW datasets).
# .npy / .png / .tif inputs work without it.
# rawpy>=0.19

# Optional: enables the Optuna TPE search in search.py (--optuna N).
# Without it the sweep falls back to an exhaustive grid search.
# optuna>=3.6

# Optional: enables downloading a pinned snapshot when freezing a Hugging Face
# model (hf_search.py --download / the GUI FREEZE button). Searching, license
# filtering and freezing the commit hash work without it (stdlib HTTP only).
# huggingface_hub>=0.23

# Optional: live testing against the Raspberry Pi CSI camera (live.py / the GUI
# LIVE TESTING button). Install ON THE PI (it ships with Raspberry Pi OS). On a
# dev machine live.py falls back to a USB webcam (OpenCV) or a simulated stream.
# picamera2>=0.3   # Raspberry Pi only
